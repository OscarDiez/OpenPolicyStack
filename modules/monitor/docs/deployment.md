# Deployment

First checkout the repo and make sure you are on the right branch ("principal" for the moment, check in gitlab).

Change to the repo folder ```connect-monitor``` and install all requirements in a new ```pipenv```-environemt by running ```pipenv run install```.

to be ontinued